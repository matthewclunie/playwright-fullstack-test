import test from "playwright/test";

test.describe("Bank account tests", () => {
  test("should register new account", async () => {});

  test("should auto add $1000 to account", async () => {});
});
