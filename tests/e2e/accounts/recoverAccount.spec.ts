import test from "playwright/test";

test.describe("Bank Account Recovery Tests", () => {
  test("should recover account", async () => {});

  test("should show account recovery error", async () => {});
});
